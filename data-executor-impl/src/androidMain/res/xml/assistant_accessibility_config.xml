<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility service configuration for AssistantRobotService.

    Scoped to Google Quick Search Box only, so the service only fires when
    Google Assistant is in the foreground — minimises battery and privacy impact.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowContentChanged|typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:packageNames="com.google.android.googlequicksearchbox" />
